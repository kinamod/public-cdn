<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Morrisons More Demonstration</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css" />
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.0.0/dist/css/bootstrap.min.css" integrity="sha384-Gn5384xqQ1aoWXA+058RXPxPg6fy4IWvTNh0E263XmFcJlSAwiGgFAW/dAiS6JXm" crossorigin="anonymous" />
        <style>
            .card {
                box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
                max-width: 300px;
                margin: auto;
                text-align: center;
                font-family: arial;
            }

            .content {
                margin: 20px;
                border: none;
                display: inline-block;
                padding: 20px;
            }

            .title {
                color: grey;
                font-size: 18px;
            }

            button {
                border: none;
                outline: 0;
                display: inline-block;
                padding: 8px;
                color: white;
                background-color: #000;
                text-align: center;
                cursor: pointer;
                width: 100%;
                font-size: 18px;
            }

            a {
                text-decoration: none;
                font-size: 22px;
                color: black;
            }

            button:hover,
            a:hover {
                opacity: 0.7;
            }
        </style>
    </head>
    <body>
      <!-- NAV BAR -->
      <div class="container-fluid" id="top-bar-container">
        <strong>
            {{#if profile}}
            <a href="/logout" class="btn btn-danger btn-lg btn-block" data-feathr-click-track="true" data-feathr-link-aids="65414312709dc1f319b80018">Logout</a>
            {{else}}
            <a href="/protected" class="btn btn-primary btn-lg btn-block" data-feathr-click-track="true" data-feathr-link-aids="65414312709dc1f319b80018">Login</a>
            {{/if}}
        </strong>
      </div>
      <!-- END NAV BAR -->
      <div id="app">
          {{#unless profile}}
            <div class="content">
              <h3>
                  This is a REALLY basic demo page. <br />
                  But please Login above, and we can show you what we can do!
              </h3>
            </div>
          {{else}}
            <h2 style="text-align: center;">User Profile Card</h2>

            <div class="card">
                <img src="{{profile.picture}}" alt="John" style="width: 100%;" />
                <h1>{{profile.name}}</h1>
                <p class="title">{{profile.user_id}}</p>
                <h2>Diet: {{profile.diet}}</h2>
                <p><button>Contact</button></p>
            </div>
            <h2>Your Nearest Stores</h2>
            (according to your Auth0 token)

            <iframe src="{{profile.morrismap}}" width="100%" height="800px" title="W3Schools Free Online Web Tutorials"></iframe>
          {{/if}}
      </div>
    </body>
</html>
